type Project @model {
  id: ID!
  projectNo: Int!
  name: String!
  allowedHours: Int!
  usedHours: Int
  status: String!
  users: [User] @connection(name: "ProjectUser")
}

type User @model {
  id: ID!
  userName: String
  project: Project @connection(name: "ProjectUser")
}
